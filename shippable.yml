language: node_js

install:
  - apt-get -y install git
  - npm install -g gulp

script: true

notifications:
  email:
    recipients:
      - mark.ward@lithium.com
    on_success: change
    on_failure: always
